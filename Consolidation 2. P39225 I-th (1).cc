#include <iostream>
using namespace std;

int main() {
    int n, seq, pos = 1;
    cin >> n;
    while (cin >> seq and pos != n and seq != -1) {
        ++pos;
        
    }
    cout << "At the position " << n << " there is a(n) " << seq << "." << endl;
}